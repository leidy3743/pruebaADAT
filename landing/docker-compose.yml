version: "3.9"

services:
  web:
    image: nginx:latest
    container_name: akila_redirect
    ports:
      - "5059:80"   # expone el sitio en http://localhost:8080
    volumes:
      - ./html:/usr/share/nginx/html:ro
    restart: unless-stopped
